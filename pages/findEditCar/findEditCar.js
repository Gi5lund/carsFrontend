import { API_URL} from "../../settings.js"

//Add id to this URL to get a single user
const URL = `${API_URL}/cars`

export async function initFindEditCar(match){
    let carId;
   /*  if(match=== undefined){
  
       carId=document.getElementById("car-id-input").value
    }
        carId=match. */
    }
  
